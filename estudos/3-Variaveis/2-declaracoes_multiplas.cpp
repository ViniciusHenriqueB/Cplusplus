#include <iostream>

using namespace std;

int main() {
    // Para declarar mais de uma variável do mesmo tipo, é só as separá-las por vírgula, por ex:
    int vidas = 3, tiros = 500, life = 100;
    /*
    Mesmo de fazer:
    int vidas = 3;
    int tiros = 500;
    int life = 100;
    */
    return 0;
}

/*
Também é possível atribuir o mesmo valor a várias variáveis ​​em uma linha:
int x, y, z;
x = y = z = 50;
*/ 